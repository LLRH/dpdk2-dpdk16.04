#include <stdio.h>
#include <math.h>
#include <unistd.h>

#define __USE_GNU

#include <sched.h>
#include <pthread.h>


//cpu_set_t ???????????1024????????????cpu??
//?????????????????????????????????cpu??
void _CPU_ZERO(cpu_set_t *set) {
    CPU_ZERO(set);
};

void _CPU_SET(int cpu, cpu_set_t *set) {
    CPU_SET(cpu, set);
};

void _CPU_CLR(int cpu, cpu_set_t *set) {
    CPU_CLR(cpu, set);
};

int _CPU_ISSET(int cpu, cpu_set_t *set) {
    return CPU_ISSET(cpu, set);
};